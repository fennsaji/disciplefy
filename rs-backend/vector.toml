[sources.docker]
type = "docker_logs"
include_labels = { "com.docker.compose.project" = "disciplefy" }

[sinks.axiom]
type = "axiom"
inputs = ["docker"]
token = "${AXIOM_API_KEY}"
dataset = "${AXIOM_DATASET}"
