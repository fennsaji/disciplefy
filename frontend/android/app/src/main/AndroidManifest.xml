<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Internet permission for OAuth and API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Network state permission -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Push notification permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required for Google Play In-App Purchases -->
    <uses-permission android:name="com.android.vending.BILLING" />
    <!-- Required for background foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required for study guide TTS audio playback in background (Android 14+) -->
    <!-- Note: dataSync type is deprecated in Android 15; download runs in main isolate so mediaPlayback covers both use cases -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Note: RECEIVE_BOOT_COMPLETED removed - FCM handles post-boot notification delivery automatically -->
    
    <application
        android:label="Disciplefy"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            
            <!-- Deep link intent filter for Supabase OAuth redirects.
                 No autoVerify — this is Supabase's domain, not ours, so we
                 can never host a matching .well-known/assetlinks.json there;
                 Play Console flags autoVerify on it as a permanently-failing
                 domain check. The intent-filter alone still lets the OAuth
                 redirect open the app. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="wzdcwxvyjuxjgzpnukvm.supabase.co"
                      android:pathPrefix="/auth/callback" />
            </intent-filter>
            
            <!-- App Links: fellowship invite deep links (HTTPS) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="app.disciplefy.in"
                      android:pathPrefix="/fellowship/join" />
                <!-- Fellowship post deep links (shared posts). -->
                <data android:scheme="https"
                      android:host="app.disciplefy.in"
                      android:pathPrefix="/fellowship" />
                <!-- Shareable learning path links.
                     Keep in sync with DEEP_LINK_PATHS in
                     api/apple-app-site-association.js so both platforms
                     open the same set of URLs. -->
                <data android:scheme="https"
                      android:host="app.disciplefy.in"
                      android:pathPrefix="/learning-path" />
            </intent-filter>

            <!-- Custom scheme for OAuth redirects (fallback) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.disciplefy.bible_study_app" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- FCM Default Notification Icon -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />

        <!-- FCM Default Notification Channel (must match a registered channel) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="daily_verse" />

        <!-- Background service for learning path downloads -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false"
            tools:replace="android:exported" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required for mailto: links to resolve. Android 11+ package
             visibility hides other apps unless they are declared here, so
             without this canLaunchUrl() reports false and launchUrl() fails —
             affecting the error report button, the settings contact and
             feedback links, and account deletion. -->
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
