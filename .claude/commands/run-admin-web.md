---
description: Start Admin Web Only
---

Start the Next.js admin web application.

Run the following command in the background:

```bash
cd /Users/fennsaji/Documents/Projects/Fenn/bible-study-app/admin-web && npm run dev
```

This will:
- Install npm dependencies (if needed)
- Start Next.js development server on port 3001

**Access Points:**
- Admin Web: http://localhost:3001

**Note:** Make sure backend is running before starting admin web.
