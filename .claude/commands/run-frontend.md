---
description: Start Frontend (Flutter Web) Only
---

Start the Flutter web application.

Run the following command in the background:

```bash
cd /Users/fennsaji/Documents/Projects/Fenn/bible-study-app/frontend && sh scripts/run_web_local.sh
```

This will:
- Install Flutter dependencies (if needed)
- Start Flutter web server
- Open Chrome browser automatically

**Access Points:**
- Frontend: http://localhost:59641 (or the port shown in output)

**Note:** Make sure backend is running before starting frontend.
